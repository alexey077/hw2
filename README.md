# hw2

комменты по большей части в коде

Исключение 2 номер

Отдельным асинхронным скриптом я отправил 100 запросов к эндпоинту, для каждого случайно выбирал задержку от 1 до 60 секунд.

По результатам -  максимальное значение delay среди запросов было 59 секунд, максимальное время отдельного запроса - примерно 59.02 секунды, а общее время- около 59.04 секунды. Благодаря асинхронности сервер обрабатывает запросы параллельно, т е пока один запрос спит, event loop не блокируется и может обрабатывать остальные
